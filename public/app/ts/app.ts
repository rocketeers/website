var app = angular.module('rocketeer', [
	'duScroll'
]);

// Controllers
//////////////////////////////////////////////////////////////////////

app.controller('AppController', Rocketeer.AppController);
